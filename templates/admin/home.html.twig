{% extends 'base.html.twig' %}

{% block title %}{{parent()}}| Administration{% endblock %}

{% block body %}
    <div class="container">
        <h3 class="mb-3">Page d'administration</h3>
        <a href="{{ path('admin_campus_list') }}" class="btn btn-secondary">Gérer les campus</a>
        <a href="{{ path('admin_gestionVilles') }}" class="btn btn-secondary">Gérer les villes</a>
        <a href="{{ path('admin_gestionUtilisateurs') }}" class="btn btn-secondary">Gérer les utilisateurs</a>
    </div>
{% endblock %}

