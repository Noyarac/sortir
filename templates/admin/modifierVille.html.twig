{% extends 'base.html.twig' %}
{% block title %}{{ parent() }} | Modifier Ville{% endblock %}

{% block body %}
    <div class="container">
        <h3>Modifier une ville</h3>
        {{ form_start(villeForm) }}
        {{ form_widget(villeForm) }}
        <button type="submit" class="btn btn-primary">Enregistrer</button>
        <a href="{{ path('admin_villes') }}" class="btn btn-secondary">Retour à la liste</a>
        {{ form_end(villeForm) }}
    </div>
{% endblock %}
