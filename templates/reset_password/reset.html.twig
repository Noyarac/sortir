{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Réinitialisation du mot de passe{% endblock %}

{% block body %}
    <div class="container" style="max-width: 60ch;">
    <h1 class="h3">Réinitialisez votre mot de passe</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Réinitialiser le mot de passe</button>
    {{ form_end(resetForm) }}
    </div>
{% endblock %}
